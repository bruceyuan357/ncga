-r requirements.txt
ruff>=0.5.0
mypy>=1.10
pytest>=8.0
waitress>=3.0.0
# Cycle 18 v2: real-browser behavior tests for the bug-fix modal + wizard.
# Keep this in dev deps only — production runtime never imports playwright.
# After install: `python -m playwright install chromium` to fetch the browser.
pytest-playwright>=0.5.0
